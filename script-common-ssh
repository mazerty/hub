su ubuntu -c "mkdir -p ~/.ssh"
su ubuntu -c "ssh-keygen -f ~/.ssh/id_rsa -N ''"
su ubuntu -c "zebr0-lookup script-common-ssh-config > ~/.ssh/config"

## lookup script-common-ssh-patch /var/tmp/ssh_config.patch
patch /etc/ssh/ssh_config /var/tmp/ssh_config.patch
